name: cwtf_app
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.1
  flutter_staggered_grid_view: ^0.4.0
  flutter_paginate: ^1.0.0
  http: ^0.13.3
  dio: ^4.0.0
  responsive_builder: ^0.4.1  
  flutter_login: ^3.0.0
  flutter_screenutil: ^5.0.0+2
  getwidget: ^2.0.5
  flutter_neumorphic: ^3.2.0
  google_fonts: ^4.0.3
  
       


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/challenge_image.jpg
    